# 공개 저장소(worktrail)에 내보내는 것 — tools/release.sh 가 읽는다.
# 한 줄에 하나. 폴더는 끝에 / 를 붙인다(그 아래 전부). 파일은 경로 그대로.
# 이 목록에 없는 새 파일은 기본적으로 비공개다. 세부 예외는 public-exclude.txt 가 한 번 더 뺀다.

# 저장소 첫 화면·패키지·로컬 실행 (main.py 는 로컬 묶음이 싣는다)
.gitignore
LICENSE
README.md
README.ko.md
casebook/
main.py
pyproject.toml

# 공개 목록 자체 (서버 조립 파일은 내보내지 않는다 — 자체 호스팅은 안내하지 않는다, 2026-09-16)
deploy/public-include.txt
deploy/public-exclude.txt

# 사용 문서
docs/ONBOARDING.md

# 공개 트리 검증과 데모·훅·릴리스 도구
tests/
tools/demo_seed.py
tools/demo_seed_data.py
tools/demo_seed_data_en.py
tools/demo_seed_data_real.py
tools/demo_shift.py
tools/demo_snapshot.py
tools/hooks/
tools/public_manifest.py
tools/release.sh

# 화면
web/join/
web/worktrail/

# Claude Code 플러그인 — 공개 저장소 루트가 곧 플러그인이자 마켓이다(#541)
.claude-plugin/
hooks/
bin/
