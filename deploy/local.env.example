# tools/deploy.sh 가 읽는 배포 대상. 이 파일을 deploy/local.env 로 복사해 값을 채운다 — local.env 는 gitignore 다.
AWS_PROFILE=<aws-profile>
CASEBOOK_INSTANCE=<ec2-instance-id>
CASEBOOK_BUCKET=<s3-bucket-for-bundles>
# CASEBOOK_HEALTH=https://<api-domain>/health   (기본값이 있다)
